# BitChat In Browser - Environment Variables
# Copy this file to .env and fill in the values

# ============================================
# IPFS Pinning Services (Optional)
# ============================================
# At least one pinning service is recommended for production deployments.
# If no credentials are provided, the deploy script will show manual instructions.

# Pinata (https://www.pinata.cloud)
# Sign up for free at: https://app.pinata.cloud/register
# Get your JWT from: https://app.pinata.cloud/developers/api-keys
# Free tier includes: 500 files, 1GB storage
PINATA_JWT=

# web3.storage (https://web3.storage)
# Sign up at: https://web3.storage
# Get your token from: https://web3.storage/account
# Free tier includes: 5GB storage
WEB3_STORAGE_TOKEN=

# ============================================
# ENS Update (Optional - for programmatic updates)
# ============================================
# WARNING: Only set these if you need programmatic ENS updates.
# For most users, manual updates via app.ens.domains are recommended.

# Private key of wallet that owns bitbrowse.eth
# NEVER commit this to version control!
# Format: 0x followed by 64 hex characters
# ENS_PRIVATE_KEY=

# Ethereum RPC URL (optional, defaults to public endpoint)
# Recommended: Use Infura, Alchemy, or your own node
# RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_API_KEY

# ============================================
# Build Configuration (Optional)
# ============================================

# Set to 'production' for optimized builds
NODE_ENV=production

# Vite base URL (usually leave empty for IPFS deployments)
# VITE_BASE_URL=

# ============================================
# CI/CD (GitHub Actions)
# ============================================
# These should be set as GitHub Secrets, not in this file:
# - PINATA_JWT
# - WEB3_STORAGE_TOKEN

# ============================================
# Manual IPFS Pinning (No API Required)
# ============================================
# If you don't want to use Pinata or web3.storage, you can:
#
# 1. Run a local IPFS node:
#    - Install IPFS Desktop: https://docs.ipfs.tech/install/ipfs-desktop/
#    - Or use CLI: https://docs.ipfs.tech/install/command-line/
#
# 2. Add your dist folder:
#    ipfs add -r --cid-version=1 dist/
#
# 3. Pin with a free pinning service:
#    - Pinata: https://app.pinata.cloud (upload via web UI)
#    - Filebase: https://filebase.com
#    - Infura IPFS: https://infura.io/product/ipfs
#    - nft.storage: https://nft.storage (for NFT-related content)
#
# 4. Or use IPFS Cluster for self-hosted pinning:
#    https://ipfscluster.io/
